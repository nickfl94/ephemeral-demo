{
  "name": "ephemeral-demo-webapp",
  "version": "1.0.0",
  "description": "Demo web application for ephemeral deployment environments",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "echo 'Build complete'",
    "test": "jest",
    "health-check": "curl -f http://localhost:3000/health || exit 1"
  },
  "keywords": [
    "demo",
    "ephemeral",
    "deployment",
    "aws",
    "terraform"
  ],
  "author": "Ephemeral Demo Team",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "helmet": "^7.1.0",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "jest": "^29.7.0",
    "supertest": "^6.3.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}